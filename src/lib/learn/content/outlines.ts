import type { Course } from "../types";

/**
 * Curriculum OUTLINES — courses whose roadmap ships but whose lessons aren't
 * authored yet (status: "outline"). All courses are now fully authored, so this
 * list is empty; add a stub here if you scaffold a new course before writing it.
 */
export const outlineCourses: Course[] = [];
